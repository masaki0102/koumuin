<div class = "inner">
<h2 class="page-heading">検索結果</h2>
<div class="exams"> 
<%# 検索結果の個数で条件分岐 %>
<% if @results.length !=0 %>
  <% @results.each do |result| %>
    <div class="exam">
    <div class="exam__list">
      <%= "自治体:#{result.government.name}" %>
     </div> 
    <div class="exam__list">
      <%= "募集職種:#{result.category.name}" %>
     </div> 
   <div class="exam__list">
      <%= "年齢制限:#{result.age}" %>
    </div> 
    </div> 
    <%# リンクして試験詳細に飛べるようにする %>

  <% end %>
<% else %>
<div class="exam">
  該当する試験はありません
</div>
<% end %>
</div>
</div>