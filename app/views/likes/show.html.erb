<div class="inner">
      <h2 class="page-heading">
        <%= " #{current_user.name}さんがいいねした試験" %>
        </h2 >
   <div class="contents"> 
     <% if @likes.exists? %>
       <% @likes.each do|like| %>
          <div class="content">
            <li class="list">
              <%= "募集職種:#{like.exam.category.name}" %>
            </li>
            <li class="list">
              <%= "日程:#{like.exam.date}"%>
            </li>
            <li class="list">
              <%= "年齢制限:#{like.exam.age}" %>
            </li>
          </div>
        <% end %>
      <% else %> 
      <div class="content">
        まだいいねした試験はありません。 
      </div>
       <% end %> 
     </div>
</div>
